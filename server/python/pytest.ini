[pytest]
markers =
    realdata: opt-in integration evidence using validated external scientific products; never enables network by itself.
    performance: opt-in timing evidence; hardware-dependent thresholds are explicit in the report/command.
