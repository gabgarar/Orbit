import { useEffect, useState } from "react";

export default function SatelliteContextMenu() { const [menu, setMenu] = useState(null); useEffect(() => { const open = (event) => setMenu(event.detail); const close = () => setMenu(null); window.addEventListener("orbit:satellite-context-open", open); window.addEventListener("orbit:satellite-context-close", close); return () => { window.removeEventListener("orbit:satellite-context-open", open); window.removeEventListener("orbit:satellite-context-close", close); }; }, []); if (!menu) return null; return <div id="satelliteContextMenu" className="open" style={{ left: menu.left, top: menu.top }}><button type="button" onClick={() => { window.dispatchEvent(new CustomEvent("orbit:satellite-context-action", { detail: { type: "visualization", id: menu.id } })); setMenu(null); }}>Opciones de visualización</button></div>; }
