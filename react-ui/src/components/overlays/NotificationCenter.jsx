export default function NotificationCenter({ notifications, onClose }) {
    return <section className="react-notification-center" role="dialog" aria-label="Notificaciones"><header><strong>Notificaciones</strong><button type="button" onClick={onClose} aria-label="Cerrar notificaciones">&#215;</button></header>{notifications.length ? <><div className="react-notification-list">{notifications.map((notification) => <article className={`react-notification-item ${notification.type === "error" ? "is-error" : ""}`} key={notification.id}><button type="button" aria-label="Descartar notificacion" onClick={() => window.dispatchEvent(new CustomEvent("orbit:dismiss-notification", { detail: notification.id }))}>&#215;</button><strong>{notification.type === "error" ? "Error" : "Informacion"}</strong><p>{notification.message}</p></article>)}</div><button className="react-notification-clear" type="button" onClick={() => window.dispatchEvent(new Event("orbit:clear-notifications"))}>Limpiar todo</button></> : <p className="react-notification-empty">No tienes notificaciones.</p>}</section>;
}
