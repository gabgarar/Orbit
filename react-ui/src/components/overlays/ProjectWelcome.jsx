export default function ProjectWelcome({ onAction, runtimeStatus }) {
    const runtimeFailed = runtimeStatus?.state === "failed";
    return <section id="projectWelcome" className="react-project-welcome" aria-label="Bienvenida de Orbit"><div className="project-welcome-card"><div className="project-welcome-orbit">O</div><div className="project-welcome-mark">O R B I T</div><h1>Welcome to Orbit</h1><div className="project-welcome-rule" /><p>Create a project to start modelling your space operations, or open an existing one.</p>{runtimeFailed && <div className="project-runtime-failure" role="alert"><strong>El visor no se pudo iniciar.</strong><span>Recarga la aplicación para volver a intentarlo.</span><button type="button" onClick={() => window.location.reload()}>Recargar aplicación</button></div>}<div className="project-welcome-actions"><button className="primary" type="button" disabled={runtimeFailed} onClick={() => onAction("new")}>New project</button><button type="button" disabled={runtimeFailed} onClick={() => onAction("open")}>Open project</button></div></div></section>;
}
